<div class="row">
    <div class="col s3">
        <p>
            <b>{{ 'inventory.label.beds'|trans }}</b><br>
            <span class="black-text">{{ inventory.beds }}</span>
        </p>
        <p>
            <b>{{ 'inventory.label.closets'|trans }}</b><br>
            <span class="black-text">{{ inventory.closets }}</span>
        </p>
        <p>
            <b>{{ 'inventory.label.tables'|trans }}</b><br>
            <span class="black-text">{{ inventory.tables }}</span>
        </p>
        <p>
            <b>{{ 'inventory.label.chairs'|trans }}</b><br>
            <span class="black-text">{{ inventory.chairs }}</span>
        </p>
    </div>
    <div class="col s3">
        <p>
            <b>{{ 'inventory.label.floor'|trans }}</b><br>
            <span class="black-text">{{ inventory.floor }}</span>
        </p>
        <p>
            <b>{{ 'inventory.label.walls'|trans }}</b><br>
            <span class="black-text">{{ inventory.walls }}</span>
        </p>
        <p>
            <b>{{ 'inventory.label.windows'|trans }}</b><br>
            <span class="black-text">{{ inventory.windows }}</span>
        </p>
        <p>
            <b>{{ 'inventory.label.doors'|trans }}</b><br>
            <span class="black-text">{{ inventory.doors }}</span>
        </p>
    </div>

    <div class="col s5">
        <p>
            <b>{{ 'inventory.label.roomsspecial'|trans }}</b><br>
            <span class="black-text">{{ inventory.roomsspecial }}</span>
        </p>
        <p>
            <b>{{ 'inventory.label.text'|trans }}</b><br>
            <span class="black-text">{{ inventory.text }}</span>
        </p>
    </div>


    <div class="col s1" style="text-align:right;">
        <a class='dropdown-trigger' href='#'
           data-target='dropdown_guests_{{ inventory.id }}'>
            <i class="material-icons grey-text">more_vert</i>
        </a>
        <!-- Dropdown Structure -->
        <ul id='dropdown_guests_{{ inventory.id }}' class='dropdown-content'>
            <li>
                <a href="{{ path('inventory_show', {'id':inventory.id}) }}" class="blue-text">
                    Show
                </a>
            </li>
            <li class="divider" tabindex="-1"></li>
            <li>
                <a href="{{ path('inventory_edit', {'id': inventory.id}) }}" class="blue-text">
                    Edit
                </a>
            </li>
        </ul>
    </div>
</div>