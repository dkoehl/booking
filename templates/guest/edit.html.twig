{% extends 'base.html.twig' %}

{% block title %}Edit Guest{% endblock %}

{% block body %}
    <a class="waves-effect waves-light btn" href="{{ path('guest_index') }}">back to list</a>
    <h3 class="section-title teal-text">Edit Guest</h3>
    <p>{{ include('guest/_form.html.twig', {'button_label': 'Update'}) }}</p>

    {{ include('guest/_delete_form.html.twig') }}
{% endblock %}
