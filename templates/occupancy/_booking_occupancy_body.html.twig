<div class="row">
    <div class="col s3">
        <p>
            <b>{{ 'occupancy.label.name'|trans }}</b><br>
            <span class="black-text">{{ occupancy.name }}</span>
        </p>

    </div>
    <div class="col s3">
        <p>
            <b>{{ 'occupancy.label.birthday'|trans }}</b><br>
            <span class="black-text">{{ occupancy.birthday ? occupancy.birthday|date("d.m.Y") : '' }}</span>
        </p>
    </div>

    <div class="col s5">

{#        {{ dump(occupancy.occupancies.id) }}#}

    </div>


    <div class="col s1" style="text-align:right;">
        <a class='dropdown-trigger' href='#'
           data-target='dropdown_guests_{{ occupancy.id }}'>
            <i class="material-icons grey-text">more_vert</i>
        </a>
        <!-- Dropdown Structure -->
        <ul id='dropdown_guests_{{ occupancy.id }}' class='dropdown-content'>
            <li>
                <a href="{{ path('occupancy_show', {'id':occupancy.id}) }}" class="blue-text">
                    Show
                </a>
            </li>
            <li class="divider" tabindex="-1"></li>
            <li>
                <a href="{{ path('occupancy_edit', {'id': occupancy.id}) }}" class="blue-text">
                    Edit
                </a>
            </li>
        </ul>
    </div>
</div>