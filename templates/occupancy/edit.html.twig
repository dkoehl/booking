{% extends 'base.html.twig' %}

{% block title %}Edit Occupancy{% endblock %}

{% block body %}
    <div class="row">
        <div class="col"><a class="waves-effect waves-light btn" href="javascript:history.back()">back to list</a></div>
        <div class="col">{{ include('occupancy/_delete_form.html.twig') }}</div>
    </div>

    <div class="card floating-container">
        <div class="card-content">
            <span class="card-title blue-text">
                <i class="material-icons">today</i> Edit Occupancy
                <div class="right right-align right-aligned">
                    <a href="javascript:history.back()">
                        <i class="material-icons small grey-text">close</i>
                    </a>
                </div>
            </span>
            <br>
            {{ include('occupancy/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
