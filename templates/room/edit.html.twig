{% extends 'base.html.twig' %}

{% block title %}Edit Room{% endblock %}

{% block body %}
    <a class="waves-effect waves-light btn"  href="{{ path('room_index') }}">back to list</a>
    <h3 class="section-title teal-text">Edit Room</h3>
    <p>{{ include('room/_form.html.twig', {'button_label': 'Update'}) }}</p>

    {{ include('room/_delete_form.html.twig') }}

{% endblock %}
