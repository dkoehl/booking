<div class="row">
    <div class="col s12 m12">
        <div class="card white">
            <div class="card-content white-text">
                <span class="card-title teal-text">Create new booking</span>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <br/>
                <div>
                    <button class="btn">{{ button_label|default('Save') }}</button>
                </div>
                {{ form_end(form) }}

            </div>
        </div>
    </div>
</div>

