{% extends 'base.html.twig' %}

{% block title %}Edit Booking{% endblock %}

{% block body %}
    <div class="row">
        <a class="waves-effect waves-light btn" href="{{ path('booking_index') }}">back to list</a>
        <div class="col">{{ include('booking/_delete_form.html.twig') }}</div>
    </div>

    <div class="card floating-container">
        <div class="card-content">
            <span class="card-title blue-text">
                <i class="material-icons">today</i> {{ 'booking.editbooking'|trans }}
                <div class="right right-align right-aligned">
                    <a href="javascript:history.back()">
                        <i class="material-icons small grey-text">close</i>
                    </a>
                </div>
            </span>
            <br>
            {{ include('booking/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>




{% endblock %}
